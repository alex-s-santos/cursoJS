//Mesmo caso do Enquanto sendo que executa ao menos uma vez - DO WHILE
let contagem = 0;
let nome = 'Alex';

do{
    contagem++
    console.log(`${contagem}. ${nome}`);
}while(nome != 'Alex'); //Executa pelo menos uma vez